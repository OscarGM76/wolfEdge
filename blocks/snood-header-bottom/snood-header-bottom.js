/**
 * loads and decorates the snood-header-bottom
 * @param {Element} block The Snood header bottom block element
 */
export default async function decorate(block) {

  // Obtenemos todos los hijos (columnas) del bloque
  const cols = [...block.children]; // Convierte HTMLCollection a Array

}
